# Roadmap

The promise: **clone → install on the Grok Bot computer → a normal chat turn
hits your hop.** `python setup.py` on a laptop is not sufficient on stock 0.30.

## Shipped

- ✅ **Stock box installer** — `tools/install-stock-box.py` wraps
  `createProtoSession`, ships `openai-hop-session.cjs`, starts the key-injecting hop
- ✅ **Honest patcher** — `apply-box-patch.py` refuses stock hosts and points at STOCK-HOST
- ✅ **One-command setup** — `python setup.py` (detect → plan → wire → verify → opens picker)
- ✅ **Selector UI** — clean per-agent dropdowns fed by your live routes; test pills; AI-assist prompt
- ✅ **Doctor reads config** — `services.json` (regenerated by setup) drives the watch table; our table kept as fallback
- ✅ **Evidence tooling** — `tools/wire-probe.py` = the 7-probe capture ladder; verdicts + full wire dumps
- ✅ **Contributor path** — CONTRIBUTING.md: probe → map → tests → PR with capture
- ✅ **CI for map PRs** — GitHub Actions runs both suites + qa.py on every push/PR (`.github/workflows/verify.yml`)
- ✅ **Wire captures shipped** — `wire-captures/` carries the raw evidence behind each map (GLM ladder first)
- ✅ **BYOK decision doc** — native BYOK vs hop lane, honest trade-offs
- ✅ Wire maps live-verified: grok, claude, gemini, glm, deepseek, qwen-local, nano-gpt
- ✅ Update survival: hot-reload maps + doctor tripwire + failure encyclopedia (F01–F18)

## Open (bounded, design settled)

- [ ] **Duck-type the remaining proto session methods** after the first
      `OPENGROK_PROBE_PROTO=1` capture from a live 0.30 box
- [ ] **More provider maps** — mechanical now: run wire-probe, follow CONTRIBUTING.
      Candidates by demand: OpenRouter passthrough quirks, Mistral, Groq, xAI native key lane.
- [ ] **OAuth flow guides per provider** — claude-shim pattern generalized; needs
      per-provider consent-screen walkthroughs (screenshots + token storage contract)
- [ ] **Picker: inline model-add** — currently reads hop routes; add "custom model"
      row that runs wire-probe from the UI and writes the map draft
- [ ] **doctor as packaged cron** — one-flag install of the scheduled watchdog
      (schtimmer/launchd/cron) instead of copy-paste VBS instructions

## Non-goals (by design)

- No key storage in this repo, ever — keys live in your hop/local secret store
- No map without a wire capture — documentation is not evidence
- No silent degradation — unverified behavior ships as documented noop, never guessed
- No Grok Bot desktop settings page — stock 0.30 has no consumer BYOK UI
